{
  "name": "valentine-showoff",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm:dev:*\"",
    "dev:frontend": "live-server --port=5500 --open=src",
    "dev:backend": "cd server && nodemon index.js",
    "start": "node server/index.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.0",
    "live-server": "^1.2.2",
    "nodemon": "^3.0.1"
  }
}